import 'package:flutter/material.dart';

class AppSpace extends StatelessWidget {
  const AppSpace({Key ? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color:Colors.white,
      height: 20,
    );
  }
}
